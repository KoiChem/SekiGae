# 非アクティブ座席への生徒移動 実装仕様（Terra向け）

## 1. 目的

42席のうち一部を空席（非アクティブ座席）として使用している座席表で、現在配置されている生徒1名を、任意の非アクティブ座席へ手動で移動できるようにする。

この操作は配置全体の再計算や他の生徒との入れ替えではなく、次の3処理を1回の不可分な変更として行う。

1. 移動先の非アクティブ座席をアクティブ化する。
2. 選択した生徒を移動先へ配置する。
3. 移動元の座席を非アクティブ化する。

移動前後で、生徒数、アクティブ座席数、非アクティブ座席数を変えてはならない。他の生徒の座席も変えてはならない。

## 2. 用語と対象範囲

- 全座席は `TOTAL_SEATS`（現行42席、6列×7行）とする。
- 「非アクティブ座席」は `inactiveSeats.has(index)` が真の座席を指す。
- `assignment[index] == null` でも `inactiveSeats` に含まれない座席は「未配置のアクティブ座席」であり、本機能の移動先にはしない。
- シャッフル後の未配置アクティブ座席を自動的に非アクティブ化する変更は今回の対象外とする。
- 対象は、現在表示中の盤面に生徒が配置され、1席以上の非アクティブ座席がある場合とする。シャッフル直後のプレビュー、確定済み配置、整列・Excel読込・履歴復元由来の盤面でも、同じ状態なら利用可能とする。
- 42席すべてがアクティブな場合は本機能を開始しない。

## 3. 操作仕様

### 3.1 操作方式

新しい常設ボタンは追加しない。PCとiPadの双方で確実に操作できるよう、移動は次の2段階のクリック／タップ方式とする。

1. 生徒がいるアクティブ座席をクリック／タップし、移動元として選択する。
2. 非アクティブ座席をクリック／タップし、移動先として指定する。

既存のHTML5ドラッグ＆ドロップによる「生徒同士の入れ替え」は変更せず残す。非アクティブ座席への移動はドラッグ操作へ依存させない。これは、iPadでの操作性と、通常時に表示範囲外となる外周の非アクティブ座席を確実に選べることを優先するためである。

### 3.2 移動元の選択中

- 移動元座席を明確な青系の枠またはリングで強調する。
- すべての非アクティブ座席を移動先候補として、青系の破線枠などで視認できる状態にする。
- 通常は `getGridBoundaries()` により省略されている外周座席も含め、選択中だけ6列×7行の42席すべてを画面上に表示する。
- 全席表示は通常画面だけの一時的な表示とし、印刷範囲、印刷プレビュー、保存データ、制約計算用の境界には影響させない。
- `showAlert()` で「移動先の空席を選んでください。同じ生徒をもう一度選ぶと解除できます。」と案内する。
- 選択中に同じ移動元を再度クリック／タップした場合は選択を解除する。
- 選択中に別の生徒がいるアクティブ座席をクリック／タップした場合は、移動元をその生徒へ切り替える。
- `Escape` キーでも選択を解除できるようにする。
- 非アクティブでもなく、生徒もいないアクティブ座席を押しても移動は実行しない。

### 3.3 `handleSeatClick()` の優先順位

既存の空席ON/OFF操作と衝突させないため、クリック処理は次の順で判定する。

1. `suppressSeatClickUntil`、印刷中、例外席選択中など、現行の操作禁止条件を先に処理する。
2. 移動元選択中なら、同じ席の再押下、別生徒への選択変更、非アクティブ座席への移動のいずれかとして処理する。
3. 移動元未選択で、押した座席に生徒がいて、かつ非アクティブ座席が1席以上ある場合は、空席切替ではなく移動元選択を開始する。
4. 上記以外は、現行のアクティブ／非アクティブ切替処理を維持する。

長押しで座席追跡モーダルを開く現行のタッチ処理と、右クリック処理を壊してはならない。長押し後のクリック抑止も維持する。

## 4. 状態遷移と不変条件

移動元を `fromIdx`、移動先を `toIdx`、現在の表示対象を `assignment = previewAssignment || seatAssignment` とする。

### 4.1 実行前条件

- `fromIdx !== toIdx`
- `!inactiveSeats.has(fromIdx)`
- `inactiveSeats.has(toIdx)`
- `assignment[fromIdx]` に生徒がいる
- `assignment[toIdx] == null`
- 印刷中、例外席選択中、シャッフル計算中ではない

前条件を満たさない場合は何も変更せず、安全に選択状態を解除または維持する。

### 4.2 実行内容

警告確認後、次の変更を同一関数内でまとめて行う。

```text
student = assignment[fromIdx]
assignment[fromIdx] = null
assignment[toIdx] = student
inactiveSeats.add(fromIdx)
inactiveSeats.delete(toIdx)
```

適用直前と直後に、少なくとも次を満たすことを確認できる構造にする。

- 配置済み生徒IDの集合と件数が不変
- `inactiveSeats.size` が不変
- `TOTAL_SEATS - inactiveSeats.size` が不変
- 移動対象以外の `assignment[i]` が不変
- 非アクティブ座席には生徒が存在しない
- 同一生徒が2席に存在しない

`inactiveSeatBackup` に `fromIdx` または `toIdx` の古い値が残っている場合は削除し、後の空席切替で生徒が重複復元されないようにする。本機能の移動処理には `inactiveSeatBackup` を利用しない。

## 5. プレビュー、確定、取消、元に戻す

### 5.1 プレビュー化

- 確定盤面から初めて移動する場合は、既存の手動入れ替えと同様に `previewAssignment = [...seatAssignment]` を作成し、「キャンセル」「座席を確定」を表示する。
- `inactiveSeats` を初めて変更する直前に、`previewInactiveSeatsBackup` が未設定なら `new Set(inactiveSeats)` を保存する。
- すでにシャッフル、整列、Excel読込などのプレビュー中なら、その `previewAssignment` に移動を適用する。
- Excel読込などによって `previewInactiveSeatsBackup` がすでに設定されている場合は上書きしない。

### 5.2 未確定状態の保存防止

プレビュー中の移動で変更した `inactiveSeats` を、外観設定などの自動保存経由で確定データとして保存してはならない。

`saveCurrentClassData()` が保存する `inactiveSeats` は、`previewInactiveSeatsBackup` が存在する間はバックアップ側（確定状態）を使い、確定後は現在の `inactiveSeats` を使う。これにより、移動後・確定前に再読み込みやバックアップ作成をしても、配置と空席状態が食い違わないようにする。

### 5.3 元に戻す

- 現在の `manualSwapUndoStack` は、入れ替えと移動を区別できる操作履歴へ拡張する。
- 入れ替えは `{ type: 'swap', idx1, idx2 }`、今回の移動は `{ type: 'relocate', fromIdx, toIdx }` 相当で保持する。
- 移動を元に戻す場合は、生徒を `toIdx` から `fromIdx` へ戻し、`fromIdx` をアクティブ化、`toIdx` を非アクティブ化する。
- 複数回の入れ替えと移動が混在しても、LIFO順に1操作ずつ正しく戻せること。
- ボタン表示は「手動入替を戻す」から「手動変更を戻す」へ変更する。

### 5.4 確定と取消

- 「座席を確定」で現在の `previewAssignment` と `inactiveSeats` を同時に確定し、履歴の `assignment` と `inactiveSeats` に保存する。
- 確定後は `previewInactiveSeatsBackup`、移動元選択、手動操作履歴をクリアする。
- 「キャンセル」では `previewAssignment` を破棄し、`previewInactiveSeatsBackup` があれば `inactiveSeats` を完全に復元する。
- キャンセル後も生徒数、確定配置、空席位置が操作開始前と一致すること。
- 移動を行ったシャッフル結果は、既存の `markPendingShuffleAsManuallyModified()` を呼び、履歴のseed表示を「手動変更あり」とする。

## 6. 必須ルール・公平性の確認

「任意の生徒を移動できる」という目的を優先し、必須ルール違反でも一律禁止にはしない。既存の手動入れ替えと同じく、警告を確認したうえで実行可能とする。

### 6.1 必須ルールの警告

移動後の空席集合を使って、次を確認する。

- 備考欄または全体ルールの座席・行・列指定
- NG隣接禁止
- 市松配置の性別区分
- 非アクティブ座席に生徒が残らないこと

違反時は既存の警告モーダルを汎用化し、タイトルを「移動の警告」、実行ボタンを「移動を実行する」とする。キャンセル時は盤面を一切変更しない。

### 6.2 公平性悪化の確認

- 移動前は現在の空席集合、移動後は移動後の空席集合から、それぞれ境界とスコア文脈を構築して比較する。
- 最大個人ポイント、ポイントがある生徒数、総合ポイントの既存比較順を維持する。
- 悪化時はタイトルを「移動の確認」、実行ボタンを「移動する」とし、既存と同等の内訳を表示する。
- 必須ルール違反と公平性悪化が同時にある場合は、必須ルール警告を優先する。

### 6.3 評価処理の注意点

現行の `calcAllowedSeatsForStudent()`、`getGridBoundaries()`、`buildScoreContext()` などはグローバルの `inactiveSeats` を参照するため、移動先が非アクティブのまま評価すると誤判定になる。評価前にグローバル状態を一時変更して戻す実装は避け、評価対象の `inactiveSet` を引数で渡せるようにする。

手動変更評価キャッシュのキーは `inactiveSeats.size` だけでは不十分である。今回の操作は空席数を保ったまま空席位置を変更するため、ソート済みの空席index列など、位置を含む署名をキーへ含める。移動適用・取消・元に戻す・履歴復元時にもキャッシュを破棄する。

## 7. 表示仕様

- 通常時の非アクティブ座席の見た目と、通常時の表示範囲は変更しない。
- 移動元選択中だけ、盤面に専用クラスを付けて42席を表示する。
- 移動元、移動先候補、現在ポインタが重なっている候補を異なる強さで示す。
- 選択中の候補表示は教員視点・生徒視点の反転後も正しい位置に見えること。
- 画面横・画面縦、PC・iPad相当幅、狭いスマートフォン幅で横スクロールや操作不能を発生させない。
- 移動完了後は通常の境界計算へ戻し、新しいアクティブ座席の範囲に合わせて盤面を再描画する。
- 完了時は `showAlert()` で「○○を a1席から f7席へ移動しました。元のa1席は空席になりました。内容を確認し、『座席を確定』を押してください。」相当を表示する。

## 8. 選択状態を必ず解除する場面

次の操作開始前には、移動元選択と専用表示クラスを解除する。

- シャッフル、市松、整列、クリア
- 履歴の盤面反映
- クラス切替、名簿反映、バックアップ読込
- 印刷プレビュー開始
- 座席の確定、プレビューのキャンセル
- 例外席選択開始

単なる画面の縦横切替、教員／生徒視点切替、ウィンドウサイズ変更では選択を失わせず、再描画後も選択表示を復元する。

## 9. 保存形式と互換性

- 保存済みJSONのスキーマは変更しない。
- 現行の `assignment` と `inactiveSeats` だけで確定結果を表現する。
- 履歴、バックアップ読込、番号コピー、印刷は既存形式をそのまま利用する。
- 旧保存データを移行する処理は追加しない。
- サンプル版の保存接頭辞と通常版の保存データを混在させない。

## 10. 変更対象

主な変更対象は次のとおり。

- `app.js`
  - 移動元選択状態
  - クリック／タップ処理
  - 一時的な42席表示
  - 移動評価と不可分な適用処理
  - 汎用化した手動操作Undo
  - 確定／取消／保存時の空席状態処理
  - 評価キャッシュキーの空席位置対応
- `styles.css`
  - 移動元と移動先候補の状態表示
  - 選択中の42席表示
  - 横・縦・狭幅での調整
- `index.html`
  - Undoボタン文言
  - 必要なら既存警告モーダルの汎用的な初期文言・ARIA調整
  - `app.js` と `styles.css` のキャッシュバスター更新
- `guide.html`
  - 「配置を手動調整する」説明へ、クリック／タップによる空席移動、元に戻す、確定／取消を追記

`sample.html` は変更せず、引き続き `index.html?sample=1` への遷移だけを行う。`meibo_sample.json` は本機能のためには変更しない。

## 11. 非対象

- シャッフルアルゴリズム自体の変更
- 生徒数に合わせた空席の自動作成
- 未配置のアクティブ座席への移動
- 移動時の生徒数・有効座席数・空席数の増減
- 複数生徒の一括移動
- 非アクティブ座席へのドラッグ＆ドロップ対応
- 保存形式のバージョン更新
- 印刷画面上での編集

## 12. 受け入れ条件

最低限、次をすべて確認する。

1. 35人・アクティブ35席・非アクティブ7席の盤面で、生徒1名を任意の非アクティブ席へ移動できる。
2. 移動後も35人・アクティブ35席・非アクティブ7席であり、他の34人は動かない。
3. 移動元が非アクティブ、移動先がアクティブになり、生徒の重複・欠落がない。
4. 通常は省略される外周の非アクティブ席も、移動元選択中は表示され、移動先にできる。
5. 確定盤面から移動するとプレビューになり、「座席を確定」で保存・履歴登録・再読み込み後の復元ができる。
6. 「キャンセル」で配置と空席位置が操作前へ完全に戻る。
7. 移動、入れ替え、移動の順に操作しても、「手動変更を戻す」で逆順に1件ずつ戻る。
8. 座席指定、NG隣接、市松違反では警告後に実行でき、警告のキャンセルでは無変更となる。
9. 公平性が悪化する移動では既存と同等の確認を表示する。
10. 42席すべてがアクティブな場合、生徒タップで移動先選択状態へ入らない。
11. 移動元選択中の再タップ、`Escape`、確定、取消、履歴反映、クラス切替、印刷開始で選択表示が残らない。
12. iPad相当のタップ操作で移動でき、座席長押しの追跡表示を妨げない。
13. 既存の生徒同士のドラッグ入れ替え、Undo、必須ルール警告、公平性警告が回帰しない。
14. 教員視点／生徒視点、画面横／画面縦、印刷の空席非表示／枠表示に移動結果が正しく反映される。
15. 番号コピー、履歴復元、バックアップ出力／読込で移動後の確定状態が保たれる。
16. 通常版と `index.html?sample=1` の両方で同じ機能が動き、サンプル操作が通常版データへ影響しない。
17. `node --check app.js` と `git diff --check` が成功する。

ブラウザのタッチエミュレーション結果は、実機iPadでのタップ、長押し、操作感の確認とは区別して報告すること。
