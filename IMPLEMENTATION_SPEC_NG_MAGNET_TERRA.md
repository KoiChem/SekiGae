# NG隣接禁止の磁石マーク表示 実装仕様（Terra向け）

## 1. 目的と結論

NG隣接禁止が有効な生徒の座席枠左上に、Unicodeの磁石絵文字 `🧲`（U+1F9F2）を表示する。

現行のNG判定をそのまま表示判定に利用できるため、保存形式やシャッフル処理を変えずに実装できる。実装上の主な注意点は、同じ左上に既存の座席・行列指定ピンが表示されることである。磁石とピンを個別に絶対配置せず、左上のマーカー群としてまとめて共存させる。

基準コードは、調査時点の `agent/ui-landscape-layout`、HEAD `8854aeb`。実装前に現在のHEADと差分を再確認すること。

## 2. 表示対象の定義

表示判定には、現行の `buildNgPairsMap(currentStudents, rulesText)` を使用する。

- `ngMap[student.id]` が存在し、その `Set.size > 0` なら磁石を1個表示する。
- NG相手が複数いても、磁石の個数は増やさない。
- 備考欄の個別指定 `NG(相手ID)` と、全体ルール `NG(左パターン,右パターン)` の両方を対象にする。
- NGルールは現行仕様どおり双方向であるため、指定を書いた側だけでなく相手側にも磁石を表示する。
- 全体ルールのワイルドカードに一致した生徒も、NGペアへ参加していれば表示する。
- 未解決の個別ID、自己指定、記法エラーなど、現行の検証で有効なNGペアにならないものには表示しない。
- 生徒がいない席と非アクティブ席には表示しない。

「相手側にも表示する」は重要である。現行の `buildNgPairsMap()` はペアを双方の `Set` に追加し、座席追跡モーダルも双方をNG対象として表示している。画面上の磁石だけを記述者側に限定すると、必須ルールの実際の効き方と表示が食い違う。

## 3. 表示範囲

- 対象は通常の座席盤面 `#seat-grid` とする。
- 通常版とサンプル版の双方で、共有されている `index.html`・`app.js`・`styles.css` から表示する。
- 教員視点・生徒視点、画面横・画面縦、確定配置・未確定プレビューのすべてで表示する。
- 印刷プレビューと実際の印刷・PDFには表示しない。現行の座席・行列指定ピンも `renderPrintLayout()` には含まれておらず、今回の磁石も同じ画面用の制約補助表示として扱う。
- 保存JSON、履歴、バックアップ、名簿データには表示用状態を追加しない。

将来、印刷にも必要という明示要望が出た場合は、`renderPrintLayout()` 側へ別途追加する。今回の実装で印刷DOMへ混入させない。

## 4. DOM構造

### 4.1 `initGrid()`

各 `.seat` に、既存の `.overall-rule-pin` と新しい `.ng-magnet` を収める `.seat-corner-markers` を1個作る。

概念上の構造は次のとおり。

```html
<div class="seat-corner-markers">
  <span class="ng-magnet" role="img" aria-label="NG隣接禁止あり" title="NG隣接禁止あり" hidden>🧲</span>
  <span class="overall-rule-pin" role="img" hidden>...</span>
</div>
```

- 磁石をDOM順でも先にし、見た目上の左上端を磁石へ割り当てる。
- 絵文字はCSSの疑似要素ではなく、実DOMの `textContent` として `🧲` を設定する。
- `.overall-rule-pin` のSVG、色、`aria-label`、`title`、座席指定は赤・行列指定は緑という現行仕様を維持する。
- マーカーは操作対象ではないため、コンテナと各マーカーを `pointer-events: none` とする。
- 詳しいNG相手は既存の座席追跡モーダルで確認できるため、磁石の可視テキストや人数バッジは追加しない。

### 4.2 ドラッグ用ゴースト

`createSeatDragGhost()` では、現行の `.seat-label, .overall-rule-pin` 個別削除を、`.seat-label, .seat-corner-markers` の削除へ置き換える。ドラッグ中のゴーストに磁石やピンを残さない。

## 5. 描画処理

### 5.1 NGマップは1描画につき1回だけ構築する

`renderAssignments()` の冒頭で全体ルール文字列を取得し、既存の `parsedOverallRules` と並べて次を1回だけ作る。

```js
const ngMap = buildNgPairsMap(currentStudents, rulesText);
```

座席ループ内で生徒ごとに `buildNgPairsMap()` を呼ばない。

### 5.2 更新関数

`updateNgMagnet(seatEl, student, ngMap)` 相当の小さな関数を追加する。

- `student` があり、`ngMap[student.id]?.size > 0` の場合だけ `.ng-magnet.hidden = false` とする。
- それ以外は `hidden = true` とする。
- 非表示時に不要な属性を動的設定する実装なら、表示解除時にそれらも消す。
- `renderAssignments()` の各座席リセット時にいったん非表示へ戻し、生徒がいる分岐で表示状態を更新する。
- 既存の `updateOverallRulePin()` は判定ロジックを変えず、マーカーコンテナ内のピンを引き続き見つけられるようにする。

これにより、クラス切替、名簿反映、全体ルール変更後の再描画、履歴復元、シャッフル、手動入替、非アクティブ席への移動、確定、取消の既存 `renderAssignments()` 経路へ自動的に追従する。

## 6. 左上位置と既存ピンとの共存

現行盤面は教員視点でグリッド全体を縦反転している。そのため、現在の `.overall-rule-pin` はCSS上では `bottom/left` に置き、`scaleY(-1)` で正立させることで、見た目上の左上になっている。生徒視点ではグリッドを横反転するため、`top/right` と `scaleX(-1)` を使用している。

この位置・反転責務を `.seat-corner-markers` へ移す。

- 教員視点: `bottom: 3px; left: 4px; transform: scaleY(-1)` 相当。
- 生徒視点: `top: 3px; right: 4px; bottom: auto; left: auto; transform: scaleX(-1)` 相当。
- `.overall-rule-pin` 自体はコンテナ内の通常配置にし、絶対位置と反転を持たせない。
- コンテナは横並びにし、磁石、既存ピンの順に表示する。
- `z-index` は現行ピンと同じ3以上を維持し、生徒名より前面に出す。
- 絵文字には `line-height: 1` を設定し、OSごとの余分な行高で上下がずれにくいようにする。

### 6.1 サイズ

- 通常幅では、磁石を18px前後、既存ピンを現行どおり22pxとする。
- 390px幅の画面縦レイアウトでは、実測した1席が約41.7px × 31.3pxであり、18px + 22pxのままでは余白を含めて収まりにくい。
- `max-width: 520px` かつ `.ui-layout-portrait` の範囲では、磁石とピンを各15〜16px、間隔1px、左余白2px程度へ縮小し、2個同時でも座席幅内へ収める。
- ピンだけ、または磁石だけの場合も同じ狭幅ルールで大きすぎない表示にする。
- 画面横を適切な横向き端末幅で使用する場合は通常サイズを維持する。

絵文字はApple、Windows、Android等で色や輪郭が異なる。ユーザー指定がUnicode絵文字なので、この差は許容し、画像・SVG・Webフォントへ置き換えない。意味は `aria-label` と `title` でも補う。

## 7. 変更対象

### `app.js`

- `initGrid()` に `.seat-corner-markers` と `.ng-magnet` を追加し、既存ピンをコンテナへ移す。
- `updateNgMagnet()` 相当を追加する。
- `renderAssignments()` でNGマップを1回構築し、各座席の磁石表示を更新する。
- `createSeatDragGhost()` でマーカーコンテナ全体を除去する。

NG解析、NGペア生成、シャッフル、手動変更時の必須ルール検証は変更しない。

### `styles.css`

- `.seat-corner-markers` の左上配置、横並び、反転補正。
- `.ng-magnet` の文字サイズ、行高、影または白い縁取り、操作透過。
- `.overall-rule-pin` から位置・反転責務をコンテナへ移す。
- 生徒視点の反転補正。
- 390px相当の画面縦レイアウト用の縮小ルール。

### `index.html`

- `app.js` と `styles.css` のキャッシュバスターを同じ新しい版へ更新する。

### `guide.html`

- NG隣接禁止の説明付近へ「座席枠左上の `🧲` は、その生徒にNG隣接禁止が設定されている印」という短い説明を追加する。
- 片方の備考欄だけに書いた個別NGでも、制約は双方へ適用され双方に磁石が出ることを誤解なく記す。
- 印刷には出ない画面上の補助表示であることを必要に応じて補足する。

### `sample.html` / `meibo_sample.json`

- `sample.html` は変更せず、`index.html?sample=1` への遷移を維持する。
- `meibo_sample.json` は変更不要。少年ジャンプのサンプルに必要な確認例がすでにある。
  - 全体ルール: `NG(3A01,3A02)`
  - 個別ルール: `3A11` の `NG(3A29)`
  - ピンとの共存: `3A11` は座席指定 `@(b1,c1,d1,e1)` も持つ
- サンプルへ実在の児童・生徒情報を追加しない。

## 8. 非対象

- NGルールの構文や検証方法の変更
- NGを片方向の制約へ変えること
- NG相手の人数、名前、線、色分けを座席枠上へ追加すること
- 磁石をクリック可能にすること
- シャッフル・手動入替・非アクティブ席移動の判定変更
- 保存形式、履歴形式、バックアップ形式の変更
- 印刷プレビュー、印刷物、PDFへの磁石追加
- 磁石絵文字を画像や独自SVGへ置き換えること

## 9. 受け入れ条件

1. 全体ルール `NG(3A01,3A02)` で、`3A01` と `3A02` の双方に磁石が1個ずつ表示される。
2. `3A11` の個別 `NG(3A29)` で、`3A11` と `3A29` の双方に磁石が1個ずつ表示される。
3. `3A11` では磁石と赤い座席指定ピンが重ならず、磁石が座席枠の見た目上の左上端にある。
4. NG相手が複数でも磁石は1個だけである。
5. NG指定のない生徒、空席、非アクティブ席には磁石が出ない。
6. 教員視点と生徒視点の双方で、磁石が見た目上の左上にあり、絵文字が鏡像・上下逆にならない。
7. 画面横、画面縦、PC、iPad相当幅、390px幅の画面縦レイアウトで、磁石・既存ピン・座席文字が不自然に重ならず、横スクロールが増えない。
8. 確定配置、シャッフルプレビュー、手動入替・空席移動後のプレビューで、同じNG生徒に表示が維持される。
9. クラス切替やルール・名簿の反映後、古い磁石が別の生徒や空席に残らない。
10. ドラッグ用ゴーストに磁石とピンが表示されない。
11. 印刷プレビューと印刷DOMに `.ng-magnet` が生成されず、印刷結果に磁石が出ない。
12. 通常版とサンプル版でコンソールエラーがなく、サンプル再読み込み時の初期化と通常版データの分離に回帰がない。

## 10. 検証

- 使用可能なNodeで `node --check app.js`。
- `git diff --check`。
- `buildNgPairsMap()` の既存挙動を利用し、個別NG、全体NG、ワイルドカード、複数相手、対象なしを確認する。
- サンプル版「少年ジャンプ」で `3A01`、`3A02`、`3A11`、`3A29` の表示と、`3A11` の磁石・赤ピン共存をDOMと目視で確認する。
- 教員視点／生徒視点を切り替え、各マーカーの位置と正立を確認する。
- 画面横／画面縦、390px幅、iPad相当幅、PC幅で確認する。
- 通常版と `index.html?sample=1` をそれぞれ開き、コンソールエラーを確認する。
- 印刷プレビューを開き、磁石が混入しないことを確認する。
- 変更した `app.js`・`styles.css` のキャッシュバスターが `index.html` と一致することを確認する。

ブラウザ確認はデスクトップ相当の証拠であり、実iPad/iPhoneにおけるApple Color Emojiの見え方は別途実機で確認する。
