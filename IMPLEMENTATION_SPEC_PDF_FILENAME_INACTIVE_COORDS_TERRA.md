# PDF候補名とシャッフル未確定空席座標 実装仕様（Terra向け）

## 1. 目的

現行の通常版・サンプル版で、次の2点を追加する。

1. ブラウザの「印刷・PDF」からPDF保存する際、保存ファイル名の初期候補を `クラス名_配置名.pdf` にする。
2. シャッフル成功後から「座席を確定」または「キャンセル」まで、非アクティブ座席に `a1` などの座席座標を薄く表示する。

この仕様での角括弧は変数を示す記法であり、実際のファイル名に角括弧は付けない。例えば、クラス名が「1年A組」、配置名が「2学期」の場合は `1年A組_2学期.pdf` を初期候補とする。

基準コードは `agent/ui-landscape-layout` の `0b7d960`。実装前に現在のHEADと差分を再確認すること。

## 2. 現行実装

- 印刷は専用PDF生成ライブラリではなく、`executePrint()` から `window.print()` を呼ぶ方式である。
- `window.print()` は引数を受け取らず、Web標準にはPDF保存名を直接指定するAPIがない。
- クラス名は `getCurrentClassName()`、現在選択中の履歴に保存済みの配置名は `getSelectedHistoryMemoForDesk()` で取得できる。
- メイン画面と印刷プレビューの教卓表示も、この2つの値を使っている。
- 座席DOMは `initGrid()` ですべての席に `.seat-label` と `getSeatLabel(i)` を作成済みで、座標の新規計算は不要である。
- 非アクティブ席では `renderAssignments()` がラベルを非表示にし、`.seat.inactive-hidden * { display: none !important; }` でも子要素を隠している。
- すべての未確定盤面は `previewAssignment` で表すが、シャッフル由来の未確定盤面だけは `pendingHistoryShuffleMeta` も存在する。整列とExcel取込は同変数をクリアする。
- `sample.html` は `index.html?sample=1` へ遷移し、通常版と同じ `app.js`・`styles.css`・`index.html` を使用する。

## 3. PDF保存名の仕様

### 3.1 候補名の構成

印刷開始時の候補名を次の順で構成する。

1. クラス名: `getCurrentClassName().trim()`
2. 配置名: `getSelectedHistoryMemoForDesk().trim()`
3. 両方がある: `クラス名_配置名`
4. クラス名だけがある: `クラス名`
5. 配置名だけがある: `配置名`
6. 両方とも空: `SekiGae`

ブラウザに渡す `document.title` には拡張子を含めない。PDF保存先を選ぶブラウザ側に `.pdf` を付加させ、`...pdf.pdf` になる可能性を避ける。

配置名は「配置名（選択中の履歴）」に保存済みの値を使用する。入力欄へ書いただけで「名前を保存」を押していない値は使わない。これにより、ファイル名と現行の教卓・印刷表示が同じ情報源になる。

配置名が空の場合は末尾に `_` を残さない。

### 3.2 ファイル名に使えない文字の正規化

`sanitizePrintFilenamePart(value)` 相当の小さな純粋関数を追加し、少なくとも次を行う。

- 前後の空白を除去する。
- 制御文字を除去する。
- Windows/macOS等で問題になりやすい `\\ / : * ? " < > |` を `_` に置換する。
- 連続する `_` を1個へまとめる。
- 末尾のピリオドと空白を除去する。
- 正規化後が空なら、その部分は空として扱う。

日本語、英数字、丸括弧、角括弧、ハイフンなどは保持する。クラス名と配置名の保存データ自体は変更しない。

### 3.3 `document.title` の一時変更

`window.print()` の直前にだけ、現在の `document.title` を退避して候補名へ変更する。

- 退避値はハードコードせず、通常版の `SekiGae` とサンプル版の `SekiGae サンプル版` をそのまま復元できるようにする。
- 既存の `afterprint` 経路で必ず元のタイトルへ戻す。
- `restorePrintScreenState()` を利用する場合、タイトル復元は同関数の `print-mode` 判定より前に行う。印刷プレビュー外から `executePrint()` が呼ばれた場合も復元できるためである。
- `window.print()` が例外を投げた場合もタイトルを復元する。
- 連続して印刷しても、1回目の候補名を「元のタイトル」として上書き保存しない。
- タイトル変更後に、既存の同期フィットと `window.print()` を実行する。既存の印刷レイアウト、向き、視点、空席枠表示は変えない。

### 3.4 ブラウザ制約

本機能が設定できるのはブラウザ印刷ダイアログの候補名までであり、ファイル名を強制してはならない。ブラウザ・OS・共有先によっては候補名を無視したり、利用者が変更できたりする。

実装コメントとガイドでは「初期候補」「対応するブラウザでは」の表現を使う。確実なファイル名で直接ダウンロードさせるためにPDF生成ライブラリへ置き換えることは今回の対象外とする。

## 4. 非アクティブ座席の座標表示

### 4.1 表示条件

座標を表示する条件は次のすべてを満たす場合だけとする。

- `previewAssignment` が存在する。
- `pendingHistoryShuffleMeta` が存在する。
- 対象座席が `inactiveSeats` に含まれる。
- 通常の盤面表示中である。

`renderAssignments()` で `#seat-grid` に、例えば `.shuffle-previewing` を次の条件で付ける。

```js
Boolean(previewAssignment && pendingHistoryShuffleMeta)
```

既存の `.previewing` を表示条件として流用しない。そうすると整列、Excel取込、確定済み配置への手動変更など、依頼範囲外のプレビューにも座標が出るためである。

市松シャッフルもシャッフル由来なので表示対象とする。シャッフル後に手動入替・非アクティブ席への移動を行っても、確定またはキャンセルまでは表示を維持する。

### 4.2 表示内容と見た目

- 表示文字は既存の `getSeatLabel(i)` をそのまま使い、小文字の `a1`〜`f7` とする。
- 新しい座標DOMは作らず、既存の `.seat-label` を再利用する。
- `.seat.inactive-hidden *` より詳細なセレクタと `display: block !important` を使い、シャッフル未確定時の `.seat-label` だけを再表示する。
- 文字は中央に配置し、非アクティブ席と分かる薄いグレーにする。目安は通常空席ラベルより小さい `clamp(10px, 1.2vw, 14px)`、`font-weight: 600`、不透明度 `0.45` 前後とする。
- 背景・枠は現在の未確定表示を維持し、座標を見せるために非アクティブ席を通常席のような枠へ戻さない。
- 教員視点では縦反転、生徒視点では横反転を打ち消す既存 `.seat-label` のtransformを維持し、文字を正立させる。
- 座標は視覚補助であり、クリック、ドラッグ、長押しを妨げないよう `pointer-events: none` を維持する。

### 4.3 手動移動表示との共存

シャッフル未確定中に生徒を選ぶと、非アクティブ席には既存の「移動先」表示も出る。座標と「移動先」を中央で重ねない。

`.manual-move-selecting` 中だけ座標を席の上端寄りへ移す、または同等の方法で「移動先」を主表示として残す。ドラッグ中の `.drag-over` 枠も維持する。座標表示のために既存のクリック／タップ移動、ドラッグ移動、Undo、確定、取消の挙動を変更してはならない。

### 4.4 表示終了

次の既存処理で `previewAssignment` または `pendingHistoryShuffleMeta` がクリアされた後、`renderAssignments()` により `.shuffle-previewing` と座標表示が消えること。

- `commitSeats()`
- `cancelPreview()`
- 整列開始・Excel取込など、シャッフル以外のプレビューへの置換
- 履歴復元、クリア、クラス切替など

印刷プレビューの `#print-seat-grid` と印刷物・PDFにはこの薄い座標を追加しない。印刷の非アクティブ席表示は、既存の「空席 隠す／空席 枠」のままとする。

## 5. 変更対象

### `app.js`

- PDF候補名の正規化・組み立て関数
- 印刷前のタイトル退避・設定と、`afterprint` での復元
- `renderAssignments()` の `.shuffle-previewing` 切替

シャッフルアルゴリズム、評価、保存データ形式、履歴形式は変更しない。

### `styles.css`

- シャッフル未確定中の非アクティブ席ラベル再表示
- 薄い座標の文字サイズ・色・不透明度
- 「移動先」表示との重なり回避
- 教員／生徒視点、縦／横画面、狭幅での調整が必要なら限定的に追加

### `index.html`

- `app.js` と `styles.css` のキャッシュバスター更新

新しいボタンや設定項目は追加しない。

### `guide.html`

- PDF保存時、対応ブラウザでは `クラス名_配置名.pdf` が初期候補になる旨
- シャッフルの未確定画面では非アクティブ席に座標が薄く表示される旨

### `sample.html` / `meibo_sample.json`

- `sample.html` は変更せず、共有実装を利用する。
- 保存形式やサンプルデータを変えないため、`meibo_sample.json` は原則変更不要。
- サンプルに実在人物の情報を追加しない。

## 6. 非対象

- 専用PDF生成・ダウンロードライブラリの導入
- ブラウザやOSを越えたPDF名の強制
- 印刷レイアウト、A4向き、視点、空席枠の仕様変更
- 配置名を印刷画面で編集する新UI
- 未保存の配置名を自動保存する変更
- 整列、Excel取込、手動変更だけのプレビューでの非アクティブ席座標表示
- 確定後の通常盤面や印刷物への非アクティブ席座標表示
- シャッフルアルゴリズム、座席座標計算、保存JSONの変更

## 7. 受け入れ条件

### PDF候補名

1. クラス名「1年A組」、保存済み配置名「2学期」で印刷すると、対応ブラウザのPDF保存名初期候補が `1年A組_2学期.pdf` になる。
2. 配置名が空なら `1年A組.pdf` となり、末尾に `_` が残らない。
3. クラス名・配置名の禁止文字が安全に `_` へ正規化され、保存データ自体は変わらない。
4. 印刷ダイアログを閉じた後、通常版は元のタイトル、サンプル版は `SekiGae サンプル版` に戻る。
5. 印刷を複数回行っても候補名が連結・重複せず、`.pdf.pdf` にならない。
6. 印刷の向き、視点、空席枠、座席内容、文字フィットに回帰がない。

### 非アクティブ席座標

1. 確定済み盤面では、非アクティブ席の座標は従来どおり表示されない。
2. シャッフル成功後、確定前だけ、すべての非アクティブ席に対応する `a1`〜`f7` が薄く表示される。
3. 「座席を確定」または「キャンセル」で座標表示が消える。
4. 整列プレビュー、Excel取込プレビュー、確定済み盤面からの手動移動プレビューには表示されない。
5. 市松シャッフルと通常シャッフルの双方で表示される。
6. 教員視点・生徒視点の双方で文字が正立し、座標と物理席の対応が正しい。
7. 画面横・画面縦、390px相当の狭幅、iPad相当幅で判読できるが、未確定の生徒名や操作表示より目立たない。
8. クリック／タップ移動の「移動先」、ドラッグのhover枠、長押し、右クリック、Undoが座標と干渉しない。
9. 印刷プレビューと実際の印刷・PDFには、今回の薄い座標が混入しない。

### 通常版・サンプル版

1. `index.html` と `index.html?sample=1` の双方で上記を確認する。
2. サンプル再読み込み時の初期化、通常版データとの分離に回帰がない。
3. 通常版とサンプル版で同じ製品コードを使用し、画面本体を複製しない。

## 8. 検証

- 使用可能なNodeで `node --check app.js`
- `git diff --check`
- ファイル名組み立て関数の純粋関数テスト
- `window.print()` の直前にタイトルが候補名となり、`afterprint` 後に元へ戻るDOMテスト
- `previewAssignment` と `pendingHistoryShuffleMeta` の組合せによる `.shuffle-previewing` の有無を確認するDOMテスト
- 通常シャッフル、市松シャッフル、整列、Excel取込、手動移動、確定、取消の回帰確認
- 通常版・サンプル版、教員／生徒視点、画面横／縦、狭幅、印刷プレビューをブラウザで確認
- PDF候補名はDOMテストだけでは証明できないため、macOSのChrome/Safariで実際の保存ダイアログを確認する
- iPad SafariでのPDF共有・ファイル保存名は実機で確認し、デスクトップブラウザの結果だけで保証しない

## 9. 実装上の注意

- `window.print()` 自体にはファイル名引数がない。候補名のために印刷経路全体を置き換えない。
- `document.title` の復元を `print-mode` 内だけに限定しない。
- 座標表示判定を `previewAssignment` だけにしない。
- `.seat.inactive-hidden * { display: none !important; }` があるため、単に `labelEl.style.display = 'block'` とするだけでは表示されない。
- `.seat-label` を複製せず既存DOMを再利用し、視点反転の既存処理を利用する。
- `sample.html` に画面本体やスクリプトを複製しない。
